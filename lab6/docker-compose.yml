services:
  lab6:
    image: ubuntu:24.04
    container_name: lab6
    tty: true
    stdin_open: true
    privileged: true
    environment:
      - DEBIAN_FRONTEND=noninteractive
    volumes:
      - .:/workspace
    working_dir: /workspace
    entrypoint: ["/workspace/setup.sh"]
